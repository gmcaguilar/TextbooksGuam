<% @posts.each do |post| %>
	<div class="jumbotron">
		<h2 class="text-center"><%= post.title %></h2>
		<p><%= post.authors %></p>
		<p><%= post.isbn10 %></p>
		<p><%= post.isbn13 %></p>
		<p><%= post.description %></p>
		<p><%= post.price %></p>
	</div>
<% end %>
