<div class="container">
	<% @posts.each do |post| %>
		<div class="post">
			<%= link_to "#{post.title}", post_path(post) %>
			<p><%= post.authors %></p>
			<p><%= post.isbn10 %></p>
			<p><%= post.isbn13 %></p>
			<p><%= post.price %></p>
		</div>
	<% end %>
</div>
