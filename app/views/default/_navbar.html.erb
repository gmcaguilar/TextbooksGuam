<nav class="navbar sticky-top navbar-expand-lg navbar-light bg-light">
  <%= link_to "TextbooksGuam.com", root_path, class: "navbar-brand" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <%= link_to "Create post", create_path, class: "nav-link" if !current_user.nil? %>
      </li>
    </ul>
    <ul class="navbar-nav">
      <li class="nav-item">
        <%= link_to "Sign up", signup_path, class: "nav-link" if current_user.nil? %>
      </li>
      <li class="nav-item">
        <%= link_to "Log in", login_path, class: "nav-link" if current_user.nil? %>
      </li>
      <li class="nav-item">
        <%= link_to "Account", current_user, class: "nav-link" if !current_user.nil? %>
      </li>
      <li class="nav-item">
        <%= link_to "Log out (#{current_user.email})", logout_path, class: "nav-link" if !current_user.nil? %>
      </li>
    </ul>
  </div>
</nav>
